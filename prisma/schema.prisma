model Markets {
  id            Int      @id @default(autoincrement())
  title         String
  description   String?
  yesResolution String
  noResolution  String
  createdAt     DateTime @default(now())
  // ... other fields
}